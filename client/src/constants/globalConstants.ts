const GLOBAL_DELAY = 200;
/**
 * Duration in miliseconds (200ms)
 */
export const modalCloseDelay = GLOBAL_DELAY;
/**
 * Duration in seconds (0.2s)
 */
export const transitionDuration = GLOBAL_DELAY / 1000 + 's';
